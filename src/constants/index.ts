export enum RestaurantType {
  VENDOR = 'vendor'
}

export enum DeliveryType {
  EXPRESS = ' ارسال اکسپرس ',
  SHOP_DELIVERY = ' پیک فروشنده '
}

export enum DeliveryFee {
  FREE = 'رایگان',
  IR_CURRENCY = 'تومان'
}

export enum Breakpoints {
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
  XL = 'xl'
}

export enum Sizes {
  CONTAINER_MAX_WIDTH = 567
}
